testMed.sh: to run lstm medication module
testLab.sh: to run lstm lab module


trainMed.sh: to train lstm medication model
trainLab.sh: to train lstm lab model

(the models included were trained with keras 2.2.0, 
you might need to train them when the keras version is different)
